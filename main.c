#include <stdio.h>
#include "arrays.h"
#DEFINE 	CHECKSIZE 4
#define int CHECK[] ={1,2,3,4}
#define int SHIFTBY 3

int main(){

void shift_element(CHECK,SHIFTBY);

for (int loop=0; loop<CHECKSIZE; loop++){
printf("%d ", *(CHECK+loop));
}

return 0;
}